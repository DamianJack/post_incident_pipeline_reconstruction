# Incident Audit Log

Source: `commit_report.log`. Root-cause descriptions are inferred from commit messages and file
context (no diffs were available), so entries marked *(inferred)* should be verified against the
actual code before being treated as final documentation.

| # | File | Manifestation (Symptom) | Root Cause | Structural Correction | Commit Hash |
|---|------|--------------------------|------------|------------------------|--------------|
| 1 | `Code/data.py` | Model validation metrics were unreliable / non-reproducible; validation set did not represent the true data distribution | Train/validation split was performed on an unshuffled, ordered index, so contiguous blocks of correlated samples (e.g. by class or acquisition order) ended up entirely in one split *(inferred)* | Introduced index randomization/shuffling before splitting, and corrected the split logic | `620e969` |
| 2 | `Code/fit.py` | Loss failed to decrease properly / training diverged or plateaued; gradients grew unexpectedly large across steps | `optimizer.zero_grad()` was omitted from the training loop, so gradients from previous batches accumulated into the current backward pass instead of being reset | Added `self.optimizer.zero_grad()` call at the correct point in the training step | `93bdede` |
| 3 | `Code/models.py` | VGGBlock (config-C, 1×1 convolutions) produced incorrect output spatial dimensions; downstream layers received mismatched channel counts, causing shape errors or silently wrong feature maps | 1×1 convolutions were using a non-zero padding value carried over from the 3×3 conv default, and channel counts were not being tracked/updated as they passed between successive conv layers *(inferred)* | Corrected padding to `0` for the 1×1 config-C convolution and fixed the running channel-count bookkeeping between layers | `42e6bd3` |
| 4 | `Code/models.py` | VGGBlock still exposed an inconsistent/unused padding argument; ResNet18 classifier produced no usable output (returned `None`), breaking the forward pass | Leftover `padding` parameter in the constructor left padding logic ambiguous; the ResNet18 classifier's `forward` method computed the final classification layer but never returned it | Removed the now-unnecessary padding parameter from VGGBlock's constructor; added the missing `return` statement in ResNet18's classifier | `089c71d` |
| 5 | `Code/models.py` | AlexNet could not be instantiated for inputs with channel counts other than the hardcoded default (e.g. grayscale or multi-spectral images) | First-layer convolution had a hardcoded `in_channels` value instead of exposing it as a constructor argument | Added an `in_channels` parameter to the AlexNet constructor and wired it into the first conv layer | `cabee5d` |
| 6 | `Code/models.py` | AlexNet's final classifier layer produced a fixed output width regardless of dataset, causing shape mismatches against the label/loss tensors for datasets with a different number of classes | `num_classes` was hardcoded in the classifier's final linear layer instead of being parameterized | Added a `num_classes` parameter to the AlexNet constructor and resized the classifier's final layer accordingly | `547caef` |
| 7 | `Code/models.py` | Activation layers behaved inconsistently across model configurations; some layers used the module class while others used a differently-typed representation | `activation_str` was being assigned/compared using a mix of module objects and strings rather than one consistent representation | Standardized activation handling to reference `nn.ReLU` consistently | `4e718f2` |
| 8 | `Code/models.py` | Activation configuration comparisons/serialization failed intermittently (e.g. when saving config or comparing against config values) | `activation_str` was assigned a non-string (class/object) value where a string identifier was expected downstream | Corrected the assignment so `activation_str` stores the string representation of the activation | `49a122e` |
| 9 | `Code/config.json`, `Code/models.py`, `Code/train.py` | Model architecture and training regularization did not reflect the values set in the config file; activation function and dropout rate were effectively ignored at runtime | Model/training code read activation and dropout settings from hardcoded defaults instead of the config object, so `config.json` values were dead configuration *(inferred)* | Rewired activation-function handling and `drop_rate` initialization to pull from `config` at model-build time | `fad0ef7` |
| 10 | `Code/config.json` | Classification head produced only 2 logits while the dataset contained 8 distinct labels, causing index-out-of-range errors or silently incorrect predictions for 6 of the classes | `NUM_CLASSES` was left at a stale placeholder value (2) instead of the true label cardinality (8) | Updated `NUM_CLASSES` from `2` to `8` | `89bf033` |
| 11 | `Code/config.json` | Regularization was absent from training runs, leaving models prone to overfitting with no configurable way to adjust it | No `drop_rate`/dropout key existed in the configuration schema | Added a dropout-rate field to `config.json` | `930dc51` |
| 12 | `Code/fit.py`, `Code/models.py`, `Code/train.py` | Model quality on imbalanced classes could not be assessed; only coarse accuracy was available, masking per-class failures | `Trainer`'s test-evaluation path computed only accuracy and had no logic to compute precision/recall/F1 | Extended `Trainer` to compute precision, recall, and F1 on the test set; renamed variables for clarity during the change | `1cdc9a5` |
| 13 | `Code/data.py` | Loss function raised dtype/shape errors (e.g. `RuntimeError` from `CrossEntropyLoss` expecting `Long` targets) or computed incorrect `num_classes`, especially visible with multi-dimensional label tensors | Labels were passed to `get_loaders` without being flattened or cast, so tensors retained an incompatible shape/dtype (e.g. `float` or extra dimensions) for the loss function | Cast train/test labels to flattened `long` tensors inside `get_loaders`; added a debug statement to surface the computed `num_classes` | `1e0a912` |
| 14 | `Code/fit.py` | Training runs could not be resumed after interruption; a crash mid-training lost all progress | `Trainer` had no mechanism to persist or restore model/optimizer state | Added checkpoint save and load methods to `Trainer`, with variable-name cleanup in `test_eval` | `7caaca4` |
| 15 | `Code/data.py` | Loading previously-saved checkpoints/data triggered `torch.load` `FutureWarning`/errors under newer PyTorch versions due to unsafe pickle deserialization defaults | `torch.load` calls in `get_loaders` did not specify the `weights_only` argument, relying on version-dependent default behavior | Updated data-loading calls to explicitly pass the `weights_only` parameter | `cd2662c` |
| 16 | `Code/data.py` | Certain classes (in the lesions dataset) were silently under- or over-represented, degrading model performance without an obvious error | No diagnostic step existed to surface class imbalance during data loading | Added class-distribution analysis/debug output inside `get_loaders` | `5e14140` |
| 17 | `config/data_config.json` | Training on the "cells" dataset converged poorly / diverged due to an inappropriate learning rate | Learning-rate value for the cells configuration was mistuned *(inferred)* | Updated the learning-rate value for the cells configuration | `ba08ffa` |
| 18 | `config/data_config.json` | Training on the "chest" dataset converged poorly / diverged due to an inappropriate learning rate | Learning-rate value for the chest configuration was mistuned *(inferred)* | Updated the learning-rate value for the chest configuration | `a8ff08b` |
| 19 | `Code/test.py` | Sweep runs failed opaquely on individual checkpoint/model failures, with insufficient logging to diagnose which configuration failed or why | `test_checkpoint` and `run_sweep` lacked structured error handling, allowing one failure to obscure or halt the full sweep without actionable logs | Refactored `test_checkpoint` and `run_sweep` to add structured logging and error handling | `db01d6c` |
| 20 | `Code/models.py`, `Code/train.py`, `config/data_config.json` | Model activation function was not configurable per architecture (VGGBlock/AlexNet), and weight decay was not applied during training, contributing to overfitting | Activation function was hardcoded in VGGBlock/AlexNet rather than parameterized, and the optimizer was not configured with a weight-decay term *(inferred)* | Added an `activation` parameter to VGGBlock and AlexNet; added `weight_decay` to `data_config.json` | `7c5b16c` |

## Notes on exclusions
The following commits were reviewed but excluded as they represent scaffolding/feature additions
rather than bug or corruption fixes: `f9b31ab` (`.gitignore` addition), `3008138` (initial config
creation), `4baa4d5`/`4baacf5`-equivalent config restructuring commit `4baa4d5` (organizational,
not corrective), `dae3769` (dependency manifest addition), `f6b2b11` (new sweep test config),
`6863abc` (new `run_sweep` feature), and `b523376` (`main` refactor to accept parameters — a
structural extension rather than a fix to a defect).
